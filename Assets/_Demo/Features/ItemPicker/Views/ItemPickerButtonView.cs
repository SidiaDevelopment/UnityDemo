using Entitas;
using UnityEngine;

public class ItemPickerButtonView : EntityView
{
    public override void Link(Contexts contexts, GameEntity entity)
    {
        base.Link(contexts, entity);
    }
}